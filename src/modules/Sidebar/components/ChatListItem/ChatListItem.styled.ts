import { ListItem } from '@mui/material';
import styled from '@emotion/styled';

export const StyledListItem = styled(ListItem)({
  padding: 0,
  display: 'flex',
  alignItems: 'center',
//   justifyContent: 'space-between',
  height: 71,
  borderTop: '2px solid #343A4F',
});
